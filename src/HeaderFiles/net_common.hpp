#pragma once

#include <boost/asio.hpp>             // how can i add all asio library?
#include <boost/asio/ts/buffer.hpp>   // is it not inside the asio.hpp?
#include <boost/asio/ts/internet.hpp> // is it not inside the asio.hpp?

#include <algorithm>
#include <chrono>
#include <cstdint>
#include <deque>
#include <iostream>
#include <memory> //?
#include <mutex>
#include <optional> //? it's not a boost?
#include <thread>
#include <vector>
